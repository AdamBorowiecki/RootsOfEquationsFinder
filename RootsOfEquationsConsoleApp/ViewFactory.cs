using RootsOfEquationsConsoleApp.View;
using System;

namespace RootsOfEquationsConsoleApp
{

    /*internal class ViewFactory
    {
        public EquationView GetView(int choose)
        {
            
            switch (choose)
            {
                case 1:
                    return new LinearEquationView();
                case 2:
                    return new SquareEquationView();
                case 3:
                    return new CubicEquationView();
                default:
                    throw new NotSupportedException();
            }
        }
    }*/
}